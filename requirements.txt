# ─────────────────────────────────────────────────────────────────────────────
# Dépendances ÉPINGLÉES (versions exactes, jamais >=).
#
# ⚠️  Cible : Raspberry Pi Zero W v1 (ARMv6) + Raspberry Pi OS, via piwheels.
#     AVANT de figer une version : vérifier qu'une wheel ARMv6 existe sur
#     https://www.piwheels.org/project/<paquet>/  (onglet « armv6l »).
#     ⚠️ La wheel est aussi taguée par version de Python :
#        - Raspberry Pi OS Bookworm → Python 3.11 (cp311)
#        - Raspberry Pi OS Trixie   → Python 3.13 (cp313)
#     Sur Trixie/Python 3.13, certaines versions n'ont de wheel que pour 3.11.
#     En particulier `cryptography` : les dernières versions n'ont pas
#     forcément de wheel ARMv6 et tomberaient sur une compilation Rust
#     (très longue / risque d'échec sur la carte).
#
#     Ces versions sont un POINT DE DÉPART validé fonctionnellement ; elles
#     DOIVENT être re-vérifiées sur une carte fraîchement flashée (cf. README).
# ─────────────────────────────────────────────────────────────────────────────

# --- HomeKit (accessoire natif) ---
HAP-python==4.9.1
# QR code d'appairage dans la console (extra [QRCode] de HAP-python)
pyqrcode==1.2.1
base36==0.1.1

# ⚠️ cryptography N'EST PAS épinglé ici, et c'est VOULU.
#    Sur ARMv6 (Pi Zero), cryptography est un paquet Rust et piwheels ne
#    fournit AUCUNE wheel armv6 → pip tenterait de compiler du Rust
#    (« can't find Rust compiler », des heures, OOM sur 512 Mo).
#    Solution : on l'installe via apt (python3-cryptography, précompilé par
#    Debian) et le venv le voit grâce à --system-site-packages (cf. install.sh).
#    HAP-python (et chacha20poly1305-reuseable) acceptent la version système.
#    zeroconf suit la même logique (python3-zeroconf via apt).

# --- Source de données Overkiz / Cozytouch ---
pyoverkiz==1.13.14

# --- Config & UX console ---
# PyYAML 6.0.1 NE COMPILE PAS sous Python 3.13 (Trixie) — bug Cython.
# 6.0.2 a les wheels cp313. Garder >= 6.0.2 sur Trixie.
PyYAML==6.0.2
questionary==2.0.1
rich==13.7.1
